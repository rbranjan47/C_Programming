#include<stdio.h>
#include<conio.h>
int main()
{
   int a,b;
   printf("Enter the value of a and b");
   scanf("%d %d",&a,&b);

     if (a>b)
     {
        printf("%d is smaller",a);
     }
     else
        {
          printf("%d is larger",b);
        }
return 0;
}
